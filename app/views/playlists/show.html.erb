<% content_for :meta_title, "#{@playlist.name} — synthwaves.fm" %>
<% content_for :meta_description, "#{pluralize(@playlist_tracks.size, 'track')} · #{format_duration(@total_duration)} on synthwaves.fm" %>
<% if (cover_track = @playlist_tracks.find { |pt| pt.track.album.cover_image.attached? }) %>
  <% content_for :meta_image, og_image_url_for(cover_track.track.album.cover_image) %>
<% end %>
<% content_for :meta_type, "music.playlist" %>

<div class="w-full">
  <div class="mb-6">
    <%= link_to "&larr; Playlists".html_safe, playlists_path, class: "text-neon-cyan hover:underline" %>
  </div>

  <div class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3 sm:gap-0 mb-6">
    <div>
      <h1 class="text-3xl font-bold font-display"><%= @playlist.name %></h1>
      <p class="text-sm text-gray-400 mt-1"><%= pluralize(@playlist_tracks.size, "track") %> · <%= format_duration(@total_duration) %></p>
    </div>
    <div class="flex flex-wrap gap-2" data-controller="clipboard" data-clipboard-content-value="<%= playlist_tracks_as_text(@playlist_tracks) %>">
      <%= button_to "Download", downloads_path(downloadable_type: "Playlist", downloadable_id: @playlist.id), class: "px-4 py-2 bg-gray-700 text-white rounded-lg hover:bg-gray-600" %>
      <button data-clipboard-target="button" data-action="clipboard#copy" class="px-4 py-2 bg-gray-700 text-white rounded-lg hover:bg-gray-600">Copy track list</button>
      <%= link_to "Edit", edit_playlist_path(@playlist), class: "px-4 py-2 bg-gray-700 text-white rounded-lg hover:bg-gray-600" %>
      <%= button_to "Delete", playlist_path(@playlist), method: :delete, data: { turbo_confirm: "Delete this playlist?" }, class: "px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700" %>
    </div>
  </div>

  <div class="bg-gray-800 rounded-lg border border-gray-700">
    <% @playlist_tracks.each do |pt| %>
      <%= render TrackRowComponent.new(track: pt.track, number: pt.position, link_title: true, link_subtitle: true) do |c| %>
        <% c.with_trailing do %>
          <% if pt.track.audio_file.attached? %>
            <%= link_to download_track_path(pt.track), class: "text-gray-400 hover:text-neon-cyan", title: "Download" do %>
              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"/></svg>
            <% end %>
          <% end %>
          <%= render "favorites/button", favorable: pt.track, favorited: @favorited_track_ids.include?(pt.track.id) %>
          <%= button_to playlist_track_path(@playlist, pt),
                method: :delete,
                class: "text-gray-400 hover:text-red-600",
                title: "Remove from playlist" do %>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
          <% end %>
        <% end %>
      <% end %>
    <% end %>
  </div>
</div>
