<span class="inline-flex items-center gap-2">
  <% if icon? %>
    <%= image_tag "icon.svg", class: icon_wrapper_classes, alt: "synthwaves.fm" %>
  <% end %>
  <% if text? %>
    <span class="font-display <%= size_classes %> bg-gradient-to-r from-blue-400 to-pink-500 bg-clip-text text-transparent glow-gradient">synthwaves.fm</span>
  <% end %>
</span>
